#include <iostream>
#include <fstream>
#include "FileHandler.h"
#include "DataAnalytics.h"
#include <math.h>
#include <sstream>
using namespace std;

int main(int argc, char **argv)
{
    ifstream in_file;
    ofstream out_file;
    string Input;
    FileHandler fh;

    fh.checkArgs(argc, argv);
    in_file.open(argv[1]);
    fh.checkInFileOpen(in_file, argv);
    out_file.open(argv[2]);
    fh.checkOutFileOpen(out_file, argv);
    //?make static?
    int numberOfRows(fh.getNumberOfRows(in_file));

    DataAnalytics d(2, 10, 7.7);
double yourmom[10] = {3,3,3,3};
for (int i = 0; i < 10; i++)
{
    cout << yourmom[i] << endl;
}

    // //to get number of columns
    // getline(in_file, Input);
    // stringstream s(Input);
    // int dimensions(0);
    // string t;
    // while (getline(s, t, ','))
    // {
    //     dimensions++;
    //     // cout << t << endl;
    // }
    // in_file.clear();
    // in_file.seekg(0);
    // // cout << dimensions << endl;

    // if (dimensions == 1)
    // {
    //     double *input = new double[numberOfRows];
    //     int i(0);
    //     string line;
    //     while (!in_file.eof())
    //     {
    //         in_file >> input[i++];
    //     }
    //     for (int i = 0; i < numberOfRows - 1; i++)
    //     {
    //         out_file << input[i] << endl;
    //     }
    //     delete[] input;
    // }
    // else
    // {
    //     double **input = new double *[dimensions];
    //     for (int i = 0; i < dimensions; i++)
    //     {
    //         input[i] = new double[numberOfRows];
    //     }
    //     int i(0);
    //     string line;
    //     while (!in_file.eof())
    //     {
    //         in_file >> line;
    //         int delim(line.find(','));
    //         for (int j = 0; j < dimensions; j++)
    //         {
    //             delim = line.find(',');
    //             string temp(line.substr(0, delim));
    //             (input[j])[i] = atof(temp.c_str());
    //             line = line.substr(delim+1);
    //         }
    //         i++;
    //     }
    //     for (int i = 0; i < numberOfRows - 1; i++)
    //     {
    //         for (int j = 0; j < dimensions; j++)
    //         {
    //             cout << (input[j])[i] << " ";
    //             out_file << (input[j])[i] << " ";
    //         }
    //         cout << endl;
    //         out_file << endl;
    //     }
    // }

    in_file.close();
    out_file.close();
    return 0;
}
